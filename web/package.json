{
  "name": "stuga-web",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:app": "cd .. && npm ci && VITE_BASE=/run/ npm run build && mkdir -p web/dist/run && cp -r dist/* web/dist/run/"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/tailwind": "^6.0.0",
    "tailwindcss": "^3.4.0"
  }
}
